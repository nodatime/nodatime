This directory is now effectively legacy - it only exists to satisfy
links from older versions of the documentation.

The main website at nodatime.org hosts all the documentation, which is
autogenerated from source on a continuous build machine.
